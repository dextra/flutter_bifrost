name: bifrost_example
description: Demonstrates how to use the bifrost plugin.
publish_to: "none"
version: 0.0.3

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  bifrost:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
